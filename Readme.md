**https://887487.github.io/_/**
---
テストデータを用いて検証し、不具合が発生していないことを確認してからファイルを出力してください

最終的にはhta化

サイドメニュー

全ページ
* 読み込み高速化
* サイドメニューから指定したExcelファイルを開く
* 使い方マニュアル を更新（FAQページにも追加）
* 別フォルダのindex.htmlから起動したとき、直下のdata.jsが読み込まれていない
* ヘッダーのボタンに白い枠が表示されることがある
* index.htmlのヘッダー右側にある yyyy/mm/dd(aaa) hh:mm:ss の表示を全ページで表示する
* 「screen-imagesフォルダ」の名称変更　→ 「SCRimgフォルダ」
* admin.htmlへジャンプするボタン（⚙）を表示。各ページ ⚙ 押下時のアクション：script.html → admin.html スクリプトタブ／mail.html → admin.html メールタブ／screen.html → admin.html 画面遷移タブ／FAQ.html → admin.html FAQタブ／hearing.html → admin.html ヒアリングタブ　を開く

index.html
* 各ページへ飛ぶボタン　既にページが開かれている場合、そのタブを表示する

script.html
* 

mail.html
* 

screen.html
* 

FAQ.html
* 

hearing.html
* 

admin.html ページ全体
* お知らせ を管理画面から編集可能にする（お知らせ.js/.txtからの読み込みを廃止する）

admin.html スクリプトタブ
* 固定テキスト（クロージングトーク）のボタン名も編集可能にしてください

admin.html ヒアリングタブ
* ヒアリングシート内にログ作成補助機能として、ボタン押下 → 複数行テキストの出力に対応したい(テンプレートに紐づけ可能な形式で)
* プレビュー　メモ/テキストボックス/プルダウン/ の幅 hearing.htmlの表示幅に合わせる（表示の統一）
* プルダウン　各項目名 の見出し右側に ” + 項目を追加” ｜ ”+「その他（手入力）」を追加” を配置。
* プルダウン　その他 と その他(手入力) で2つ存在する
* プルダウン　編集時、”＋「その他（手入力）」を追加” を押下した分だけ その他 が挿入されてしまう（1個あれば十分）
* チェックボックス　チェックを付けた順番で出力されているが、項目の並び順に従って出力してください
* トグルボタン　複数選択のチェック有無に関わらず、複数選択が許可されている（小項目）
* トグルボタン　中項目から先頭記号を外す
* メモ に独立したクリアボタン（メモの内容のみ消すためのボタン）

admin.html サイドメニュータブ
* ショートカットキー一覧の形式で表を追加できるようにしてください
* ショートカットキー一覧　キー の列に入力されているキーを判定する
* フォネティックコード/メールドメイン一覧　削除ボタンを表示しない
---
プロファイル化

---
分析ページを作成
* CMリスト　社員番号：姓名　の形式で保存
* Excelインポート時にそのデータがいつの分かを指定する
* 姓名 を 社員番号 に置換して表示する

* 取り込むシートの配列　名前｜合計応答数｜平均応答数｜CPH｜AHT｜合計通話時間｜ATT｜合計後処理時間｜ACW｜合計保留時間｜	HOLD/acd｜HOLD/hold｜保留数｜保留率｜実質CPH｜平均稼働率｜平均有給稼働時間｜平均ユーザー対応時間｜平均離席休憩時間｜平均食事時間｜
<img width="1601" height="22" alt="image" src="https://github.com/user-attachments/assets/c5adefae-9d2c-4ea8-abba-8f0ce405e433" />

---
* ヒアリングシートは対応履歴の作成補助としても活用するため、入力を必要としない見出しテキストも出力する必要がある
* テンプレートの中でボタン選択時に表示する内容の切り替えに対応してほしい
* ウィンドウサイズを狭めた時、ボタン同士が重なってしまう
---
* 管理者用の定型文ボタン の表示を考える → admin.htmlにのみ表示？
* HS登録 → HS部分に遷移先となる画像をライブラリからD&D
* ［CTRL］+［；］で日付入力　／　［CTRL］+［：］で時間入力 に対応する
---
* 別PCでadmin.htmlを開いたとき、画像ライブラリが空になっている（screen.html と パターン には反映されている）※jsonから個別で画面遷移をインポートすれば解消される
* ./screen-images　の画像数がライブラリ内の数より多い（なぜ？）
* 別PCからadmin.htmlを開いたとき、画像ライブラリが空になる
* 未分類に戻した後、フォルダを削除する　→　削除されていない
* jsonインポート後、保存して反映 を実行しても ./screen-images に画像データが書き出されない
* 画像ライブラリが読み込まれない
* ①PC(A)で画面遷移データ作成 → ②保存して反映 を実行 →③出力された screen-image フォルダとjsonをGitHubにアップロード ※jsonは容量の問題でZipに圧縮 → ④PC(B)でGitHubからファイルをDL → ⑤PC(B)でadmin.htmlを開き、フォルダ接続 → ⑥画面遷移のjsonを個別インポート → ⑦ 保存して反映 
* /screen-images 内のファイル数が必要数より増える
* PC(A) で作成したフォルダが反映されていない
* 同じ画像に対して、別のIDを付与して./screen-images 内のファイルが無駄に増えていたりしないか
* 画像に対して、付与するIDは固定化、必ずそのファイルを参照する
* screen-image のフォルダ内を正確に読み込む（フォルダ情報等）
* IDBを完全廃止し、実ファイルの参照に切り替え
